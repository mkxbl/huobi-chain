import { muta, GENESIS, delay, client, accounts } from "./utils";

describe("metadata service API test via muta-sdk-js", () => {
    test("test get metadata", async () => {
        // const res = await client.queryService({
        //     serviceName: "metadata",
        //     method: "get_metadata",
        //     payload: ""
        // });
        // const ret = JSON.parse(res.ret);

        // const expected = JSON.parse(GENESIS.services[1].payload);
        // expect(ret).toStrictEqual(expected);
    });
});